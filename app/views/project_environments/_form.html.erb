<%= form_with(model: project_environment, local: true) do |form| %>
  <% if project_environment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_environment.errors.count, "error") %> prohibited this project_environment from being saved:</h2>

      <ul>
        <% project_environment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :is_public %>
    <%= form.text_field :is_public %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

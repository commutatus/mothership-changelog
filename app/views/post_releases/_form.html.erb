<%= form_with(model: post_release, local: true) do |form| %>
  <% if post_release.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post_release.errors.count, "error") %> prohibited this post_release from being saved:</h2>

      <ul>
        <% post_release.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.text_field :post_id %>
  </div>

  <div class="field">
    <%= form.label :project_environment_id %>
    <%= form.text_field :project_environment_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
